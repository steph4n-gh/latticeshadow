[pytest]
testpaths = packages/db/tests packages/cli/tests
addopts = --import-mode=importlib -m "not hardware and not model"
markers =
    hardware: requires native macOS security services and may create temporary Keychain keys
    model: downloads and runs the pinned local embedding model
