# Two-hour local source soak on predecessor `b603442`

The opt-in `soak-candidate` profile completed on source commit
`b603442dfb471cf22f9a561f1b034a6582f830d9` with process exit 0. The
validation script SHA-256 was
`a102971832a53b397d53e2ab7355a72840235f69f660f82ea8924dc53cf33c15`.
It used a disposable synthetic vault and hash embeddings on a local Apple
Silicon Mac (Mac16,8, macOS 27.0, 24 GiB RAM, Python 3.14.4), **not** the
Mac mini or packaged app. This source revision precedes later CLI and desktop
changes; the result is scoped to canonical save/search/delete/reopen behavior
in `b603442`, not exact-current-runtime E1 clearance.

The driver ran for 7,200.2 seconds (7,200.85 seconds including final checks),
with a 0.5-second operation interval, a cap of 256 live events, and 238
fsynced samples roughly 30 seconds apart. It recorded 4,973 synthetic
captures, 3,539 searches, 4,718 deletions and 231 vault reopens. A reopen
recreates the vault handle; every tenth reopen also used a separate reader
process. This is not a daemon or macOS reboot test.

After the full elapsed period, the driver reopened the canonical vault,
verified every remaining live ID, and verified a sample of 256 deleted IDs
was unavailable. Its descriptor/thread/child growth assertions passed.

| Resource | First | Last | Sampled range |
| --- | ---: | ---: | ---: |
| Open descriptors | 5 | 5 | 5–5 |
| Threads | 1 | 1 | 1–1 |
| Child processes | 0 | 0 | 0–0 |
| Process RSS | 229.9 MiB | 105.1 MiB | 98.0–236.0 MiB |
| Vault files on disk | 0.2 MiB | 2.2 MiB | 0.2–2.2 MiB |

RSS did not rise monotonically over this run. Disk use increased while
tombstones accumulated; a two-hour run does not establish an indefinitely
bounded disk footprint. The standard real-model performance and packaged-app
results are separate evidence.

The sanitized raw report and progress log remain ignored by Git at
`benchmark_results/evidence-b603442-soak.json` (SHA-256
`c1371ac33b08bc90aed8bc43c1be8deb91047b3c2165377a929b85b57f9c54a9`)
and `benchmark_results/evidence-b603442-soak/soak-progress.jsonl` (SHA-256
`b9a59c34407f7b7f35faa1e621e68b2bdaae31bf3dc625b7a569b5fdd82e819a`).
They contain counts and resources, not remembered text, keys or personal
paths. Prior attempts on older candidates were stopped when those candidates
were superseded and are not counted as passes.
